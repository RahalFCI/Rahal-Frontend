 the backend is running on docker right. you can check the repo at E:\projects\Rahal-Backend i merged the latest branch with main. we need to introduce the latest feature to our application, social media. this is a comprehensive update, and we have another problem, you can check the commit history to see who was responsible for the social media branch but everything written by that person has to be taken with a grain of salt, he's a complete idiot and some of the functions on there are possibly not even working. this phase will require comprehensive testing and reviewing of the backend code itself unlike other phases where we just implement endpoints. break this down into subtasks and output progress and findings into md files every step so we can cleanly resume at any point. after review of the backend logic we need to implement this on our frontend side. please do not assume anything works on the frontend without trying it first. run an emulator and test everything yourself. 